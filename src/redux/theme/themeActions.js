import { Theme_Dark, Theme_Light } from "./themeType"

export const themeDark = () => {
  return {
    type: Theme_Dark,
  }
}

export const themeLight = () => {
  return {
    type: Theme_Light,
  }
}